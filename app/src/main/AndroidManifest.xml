<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileappproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity"/>

        <activity android:name=".MainMangaListActivity"/>
        <activity android:name=".MainAnimeListActivity" />
        <activity android:name=".UpdateDeleteAnime" />
        <activity android:name=".animeList" />
        <activity android:name=".mangaList"/>
        <activity android:name=".UpdateDeleteManga"/>
        <activity android:name=".MainMangaActivity"/>
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogInActivity"/>
        <activity android:name=".MainAnimeActivity"/>
    </application>

</manifest>